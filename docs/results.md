# Results

_Generated by `scripts/evaluate.py` from `results/`. All scores are against human labels on the 3,080-query test split. **gold**-trained rows are the supervised reference; **teacher**-trained rows are the distilled models._

Teacher (`gpt-oss-120b`, zero-shot): accuracy **0.959** on the 340 test queries labelled so far (11% — intent-sorted, so partial coverage is not a random sample). Agreement below is measured on those rows.

| model | params | trained on | n train | acc vs gold | macro-F1 | agree w/ teacher | ECE | p50 / p95 ms |
|---|---:|---|---:|---:|---:|---:|---:|---:|
| tfidf+lr | — | gold | 10,003 | **0.913** | 0.913 | 0.885 | 0.086 | 1.6 / 2.0 |

## Cascade preview — escalate the least-confident queries to the teacher

Accuracy of the mixed system when the student's least-confident X % of test queries are answered by the teacher instead. Needs teacher labels for those rows; blank until the test split is fully labelled.

| model | trained on | 0 % | 5 % | 10 % | 20 % | 30 % |
|---|---|---:|---:|---:|---:|---:|
| tfidf+lr | gold | 0.913 | — | — | — | — |
