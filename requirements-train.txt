# Students that need PyTorch. Installed separately to keep the base venv small:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
#   pip install -r requirements-train.txt
sentence-transformers>=3.0
setfit>=1.1
onnx>=1.16
onnxruntime>=1.18
# Only for denoise.py --variants tabaudit,tabaudit_suspected (roadmap 1b.5)
tabaudit==0.3.1
