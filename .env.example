# Copy to .env (git-ignored). All free, no card. Catalogues rotate — check what each key can use.
GROQ_API_KEY=            # https://console.groq.com/keys        main teacher (gpt-oss-120b), ~200k tokens/day
OPENROUTER_API_KEY=      # https://openrouter.ai/keys           second teacher (:free models, ~50 req/day)
NVIDIA_API_KEY=          # https://build.nvidia.com             gpt-oss-120b, optional extra throughput
GEMINI_API_KEY=          # https://aistudio.google.com/apikey   fallback
