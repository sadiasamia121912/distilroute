# The student-only service image (Dockerfile): an ONNX router behind FastAPI.
# No torch, scipy, pandas or scikit-learn: the service imports only what an `onnx` model needs.
fastapi==0.141.1
uvicorn==0.53.0
numpy==2.5.3
onnxruntime==1.30.0
transformers==5.17.0
tokenizers==0.23.2
